# CLAUDE.md — tigerOrchid 프로젝트 메모리

이 저장소는 **WORD QUEST 선생님·원장용 랜딩페이지**(정적 사이트)다.
운영사는 **타이거오키드**. 학생용 게임 앱과는 **별개의 페이지**다.

---

## 1. 이 페이지의 목적 (절대 흐리지 말 것)

기사·소문·검색으로 유입된 **선생님 / 원장 / 학부모**가 도착해서
→ **신뢰**를 얻고 → **"도입 문의"를 남기게** 하는 것.

- 전환 목표는 **문의 폼 제출** 하나다.
- **학생 가입은 목표가 아니다.** 학생 앱 링크는 푸터에만, 눈에 띄지 않게.
- 고객은 교사·원장 — 유치원 느낌이 나면 실패다.

### ★ 무엇을 대체하는가 (틀리면 페이지 전체가 어긋난다)

**WORD QUEST 는 아이들이 "단어를 외워오는 과정"을 대체한다.
단어 시험이나 채점을 대체하는 것이 아니다.**

- 선생님은 **시험을 지금 보시던 대로 보신다.** 평가 방식은 건드리지 않는다.
- 바뀌는 건 시험 **전**이다 — 아이들이 단어장을 펴는 대신 이걸 켠다.
- 선생님이 얻는 것은 "채점 결과"가 아니라 **"시험 전에 누가 얼마나 외워왔는지"** 다.
- 평가 체계를 바꾸지 않아도 된다는 점이 도입 저항을 없애는 핵심 문장이다.
  달라지는점 섹션 리드와 FAQ 첫 항목에 있다. **지우지 말 것.**

한때 "시험지 만들기·채점을 대신한다"로 써 두었다가 정정했다.
2번 섹션 스펙 카피에 "시험지 만들고, 채점하고"가 나오는 건 **선생님의 통증 묘사**이지
제품이 그걸 대신한다는 뜻이 아니다. 이 문장을 근거로 카피를 확장하지 말 것.

## 2. 톤 규칙 (위반 시 리뷰 리젝)

- **헤드라인에서 "게임" 단어를 앞세우지 않는다.** 스크린샷과 함께 보여줄 때만 자연스럽게 사용.
- 몬스터/게임 아트는 **3번(해결)·6번(학생들의 말) 섹션에만**. 페이지 상단은 어른의 신뢰 영역.
  **히어로의 데모 슬라이드도 예외가 아니다** — 여기는 선생님 대시보드·세팅 화면만 들어간다.
  (`tools/verify.mjs` 가 자동으로 잡는다)
- 숫자(69%, 110명)는 오렌지 볼드 1.5배로 강조하되, **24px 이상일 때만**.
  문장 안에 섞인 숫자는 잉크 볼드 — 본문 크기 오렌지는 대비가 안 나온다.
- 오렌지는 그 외엔 게임 요소 배지 배경·형광펜 밑줄에만.

### 정직성 규칙 (이 페이지는 신뢰로 판다)

- **그려낸 화면을 "실제 화면"이라고 부르지 않는다.** 자리표시자(.svg)를 쓰는 동안은
  목업에 `예시 화면` 배지를 붙이고 수치가 실제가 아님을 밝힌다.
- **실제 캡처를 쓸 때는 반대로, 가린 부분이 있다는 사실을 밝힌다.**
  현재 스크린샷은 실제 사용 화면이고 학교명·학생 이름을 모자이크했다.
  "실제 사용 화면입니다. 학생 이름과 학교명은 가렸습니다." 문구가 히어로·3번 섹션에 있다.
  **이 문구를 지우면 검증이 떨어진다** — 학생 정보를 조심히 다룬다는 신호이자 정직성 근거다.
- **학교를 특정하지 않는다.** 학교명이 페이지에 노출되면 반이 특정되고,
  가린 학생 이름도 그 반 안에서는 순위·단어 수로 되짚어진다. 언론 보도 링크나
  선생님 코멘트를 넣을 때 학교명이 딸려 들어오지 않는지 확인할 것.
- **없는 주소로 링크하지 않는다.** 확정 전에는 링크를 두지 않고 TODO 로 남긴다.
- **문의가 조용히 사라지면 안 된다.** 전송 실패나 메일 앱 미실행 시 "보냈습니다"라고
  말하지 않고, 작성한 내용을 화면에 꺼내 복사할 수 있게 한다.

## 3. 디자인 시스템 — 타이거오키드

> "난초의 보라로 단정하게, 호랑이의 오렌지로 생기 있게."

핵심 토큰 (`assets/css/tokens.css`가 단일 진실 소스):

| 역할 | HEX | 토큰 |
|---|---|---|
| 오키드 퍼플(주색) | `#7C3AED` | `--c-purple` |
| 딥 오키드(hover) | `#5B21B6` | `--c-purple-deep` |
| 라이트 오키드(틴트) | `#F3EEFF` | `--c-purple-tint` |
| 타이거 오렌지(액센트) | `#F97316` | `--c-orange` |
| 잉크(본문) | `#211A33` | `--c-ink` |
| 그레이 퍼플(보조) | `#6B6480` | `--c-ink-soft` |
| 오프화이트(배경) | `#FCFAFF` | `--c-bg` |
| 라인 | `#EAE4F5` | `--c-line` |

- **비율 엄수:** 배경·화이트 80% / 퍼플 12% / 오렌지 **5% 이하** / 기타 3%
- **오렌지는 작은 본문 텍스트 색으로 금지** (흰 배경 대비 2.9:1). 24px+ 볼드 숫자·배지·밑줄만.
- 타이포: 헤드라인 Paperlogy, 본문 Pretendard(400/600/700). 히어로 40/28, 섹션타이틀 28/22, 본문 17/16, 캡션 14, 행간 1.65
- 버튼 radius 12 / 카드 radius 16 / 섹션 패딩 96(모바일 64) / 배경 오프화이트↔화이트 교차
- **형광펜 밑줄은 히어로 "하고 싶어하게" 한 곳만** (호랑이 줄무늬 은유, 유일한 그래픽 장치)
- **난초 라인 드로잉은 문의 섹션 배경 하나만** (퍼플 8% 투명도)

자세한 스펙: `docs/DESIGN-SYSTEM.md`

## 4. 하지 말 것

- 원색 남발 / 무지개 그라데이션 / 애니메이션 과다
- 호랑이 무늬 패턴 배경 (줄무늬는 형광펜 밑줄로만 은유)
- 스톡 사진 속 외국인 교실 (실제 화면이 최고의 이미지)
- 난초·형광펜 모티프를 다른 섹션에 반복
- 빌드 스텝·프레임워크 도입 (정적 페이지 유지)

## 5. 구조

```
index.html            히어로/공감/해결/기능/★달라지는점/신뢰/학생의말/만든사람/
                      요금/★도입절차/★FAQ/문의/푸터
                      ★ = 구매자(선생님·원장) 관점 평가로 추가한 섹션. 스펙 10개는 그대로 있다.
privacy.html          개인정보처리방침 (푸터 필수 링크)
assets/css/tokens.css 디자인 토큰 — 색·타이포·간격의 단일 진실 소스
assets/css/style.css  레이아웃·컴포넌트
assets/js/config.js   ★ 폼 엔드포인트 설정. 운영자가 여기만 고치면 된다
assets/js/main.js     슬라이더·폼 제출·스크롤
assets/img/           실제 스크린샷 PNG 5장 (마스킹 + 브라우저/시스템 크롬 제거,
                      540×1063 로 통일), 몬스터, 제작자, OG, 파비콘
tools/verify.mjs      자동 검증 + 100점 채점
tools/form-check.mjs  ★ 문의가 진짜 도착하는지 점검 (npm run form:check)
tools/render-og.mjs   OG 이미지(1200×630 PNG) 재생성
docs/                 PLAN.md / DESIGN-SYSTEM.md / VERIFICATION.md
```

## 6. 개발 규칙

- **정적 사이트다.** 빌드 없음. 파일을 그대로 호스팅한다.
- 로컬 확인: `npm run dev` (`python3 -m http.server 8080`)
- 검증: `npm run verify` → **85점 미만이면 머지하지 않는다.**
- OG 재생성: `npm run og`
- 색을 새로 쓰고 싶으면 먼저 `tokens.css`에 토큰을 만든다. 하드코딩 HEX 금지.

## 7. 폼 동작 (중요)

`assets/js/config.js`의 `FORM_MODE`로 갈린다.
- `'formsubmit'` **(기본값)** — formsubmit.co 중계로 운영자 메일함에 바로 전달.
  응답을 읽을 수 있어 **접수 여부를 확인할 수 있는 유일한 모드**다.
- `'google'` — Google Form에 숨은 iframe POST. `formResponseUrl` + `entry.*` ID 필요.
- `'firestore'` — 준비되면 `inquiries` 컬렉션 저장으로 전환 (어댑터 자리 마련됨).
- `'mailto'` **(폴백)** — 위 설정이 비면 자동으로 내려감. 메일 클라이언트로 넘긴다.

**문의는 절대 조용히 사라지면 안 된다.** 그래서:
- FormSubmit 응답의 `success` 가 `true`/`"true"` 로 확인될 때만 "접수되었습니다"라고 말한다.
  HTTP 오류·미활성화·네트워크 끊김·15초 타임아웃은 전부 **실패로 처리**하고
  작성한 내용을 복사 상자에 꺼낸다. 실패 원인은 콘솔에만 남긴다(방문자에게 기술 문구 금지).
- 실패 뒤 재시도해서 성공하면 복사 상자를 치운다.
- 구글 폼 모드는 응답을 읽을 수 없어 실패를 감지하지 못한다. 그래서 성공 문구에
  "며칠 내 연락이 없으면 다시 보내주세요"가 붙어 있다. **지우지 말 것.**

**FormSubmit 은 최초 1회 주소 확인이 필요하다.** 확인 전 첫 요청은 접수되지 않으므로
배포 전에 반드시 `npm run form:check` 로 `✔ 접수됨` 을 봐야 한다.

**FormSubmit 은 `Origin` 없는 요청을 거부한다.** ("Make sure you open this page through
a web server") 그래서 두 가지를 기억할 것:
- `index.html` 을 **더블클릭해서 `file://` 로 열면 폼이 동작하지 않는다.** 확인은
  `npm run dev` 로 띄운 뒤에 하거나 배포된 주소에서 한다. (이때도 실패로 처리되어
  작성한 내용은 복사 상자로 나온다 — 조용히 사라지지 않는다.)
- 브라우저는 `Origin` 을 자동으로 붙이지만 Node 는 안 붙인다. 그래서
  `tools/form-check.mjs` 는 `index.html` 의 **canonical** 을 읽어 `Origin`/`Referer` 로 실어 보낸다.
  배포 도메인을 바꾸면 canonical 만 고치면 이 점검도 따라간다.

폼에는 `name="_honey"` 허니팟이 있다 (`.form__hp`, 화면 밖 · `tabindex="-1"` · `aria-hidden`).
사람은 절대 못 채우므로 높이를 0으로 둬서 "터치 타깃 44px" 검사에서 빠진다. **되돌리지 말 것.**

접수 경로를 바꾸면 `privacy.html` 5항(처리 위탁 및 국외 이전)의 수탁자 표를 함께 고친다.
제3자를 거치는 사실과 국외 전송은 반드시 밝힌다.

## 8. 남아 있는 사람 몫

1. ~~실제 앱 스크린샷으로 교체~~ **완료** — `assets/img/screen-*.png` 5장이 실제 캡처다
   (전투·도감·오답TOP10+랭킹·학생별현황·단어배포). 마스킹 원본 좌표는 git 이력 참조.
2. ~~`config.js`에 Google Form URL/entry ID 입력~~ **폼 연결 완료 (FormSubmit 중계)** —
   남은 건 **최초 1회 주소 확인**뿐이다. `npm run form:check` → 메일 링크 클릭 →
   `npm run form:check` 재실행해 `✔ 접수됨` 확인. **이걸 하기 전엔 문의가 한 건도 안 들어온다.**
   확인 후 FormSubmit 이 주는 별칭으로 `formsubmit.target` 을 바꾸면 소스에서 메일 주소가 사라진다.
   또한 `privacy.html` 5항의 "FormSubmit (formsubmit.co)" 에 **운영 법인명·소재 국가**를 확인해
   채워 넣을 것 (현재는 국외 전송이라는 사실만 밝혀 둔 상태).
3. 1분 데모 영상 완성 → 히어로 슬라이드를 유튜브 unlisted 임베드로 교체 (`index.html`의 `<!-- VIDEO SLOT -->`)
4. 9월 지표(참여율 %, 누적 학습 단어 수, 선생님 코멘트, 언론 보도 링크) → 5번 섹션 `<!-- SEPT SLOT -->`에 삽입
5. 개인정보처리방침의 사업자 정보 확정
6. 배포 도메인 확정 → `index.html` 의 canonical / og:url / og:image / twitter:image 4곳 교체

확정된 사실 (페이지에 반영 완료):
- 학생 앱 주소: `https://word-quest-fywr.vercel.app/` (푸터 링크)
- 광고·결제 유도 **없음** / 학습 기록 내보내기 **아직 안 됨** — 둘 다 FAQ 에 명시

### 설문 지표 (2026년 7월, 5개 반 학생 110명 · 113건 응답)

원본 CSV 에는 **학생 실명이 있어 저장소에 넣지 않았다.** 집계값과 익명 인용만 싣는다.
스펙의 "110명 설문 69%" 는 원본에서 검산해 정확히 일치함을 확인했다(78/113 = 69.0%).

| 지표 | 값 | 근거 |
|---|---|---|
| 기존 암기 방식보다 낫다 | **69%** | 훨씬 낫다 33% + 조금 낫다 36% |
| 정해진 분량 초과 학습 | **50%** | 초과 이유를 적은 학생 57/113 |
| 없어지면 아쉽다 | **79%** | 매우 19% + 조금 60% |
| 숙제 아니어도 다시 쓰겠다 | **75%** | 계속 34% + 시험기간 41% |
| 예전 방식이 낫다 | **7%** | 페이지에 **그대로 밝힌다** |
| 개학 후 안 쓸 듯 | **25%** | 페이지에 **그대로 밝힌다** |
| 추천 점수 | 평균 7.2 / 10 | 접기 안에 게시 |

### ★ 숫자를 두는 층 (섞지 말 것)

**헤드라인 층에는 확실한 세 개만.** 69%(더 낫다) · 50%(자발적 초과 학습) · 79%(없어지면 아쉽다).
카드를 늘리면 신뢰가 아니라 잡음이 된다. 여기에 부정 수치를 올리지 말 것 —
원장 머리에 남는 건 근거가 아니라 의심이다.

**나머지 분포는 전부 "설문 전체 데이터 보기" 접기 안에.** 75%·7%·25%·7.2점,
그리고 좋게 답하지 않은 학생의 인용까지 빼지 않고 넣는다.

**부정 응답을 지우지 말 것.** 접기 안으로 내린 것이지 없앤 게 아니다.
접기 바로 위의 "모든 학생에게 맞지는 않았습니다 …" 한 줄(`.proof__limit`)이
그 신호를 눈에 보이는 층에 남겨둔다. **이 한 줄과 접기를 함께 지우면 정직성 근거가 사라진다.**

**모든 수치는 페이지 위에서 계산이 보여야 한다.** 카드 설명과 접기 표에
"훨씬 낫다 33% + 조금 낫다 36% (78/113)" 처럼 분자·분모와 합산 근거를 함께 적는다.
분모는 학생 수 110명이 아니라 **응답 113건**이다. 헷갈리게 쓰지 말 것.

## 9. 구매자 관점 원칙 (스펙 준수와 별개로 지킬 것)

`npm run verify` 100점은 **스펙을 지켰다**는 뜻이지 **팔린다**는 뜻이 아니다.
구매자(학교·학원 선생님)가 결정하려면 아래가 반드시 페이지에 있어야 한다.
지우거나 흐리지 말 것.

- **실무 부담이 얼마나 작은지** — 도입 3단계와 소요 시간. 특히 "학생 계정을 선생님이
  만들지 않아도 된다"는 문장. 이게 없으면 "일이 커지겠네" 하고 닫는다.
- **부정행위 방지** — "서버가 관측한 값이라 학생이 못 바꾼다". 숙제 앱의 1번 질문이다.
  기능 섹션의 `.note` 블록과 FAQ 양쪽에 있다.
- **기기 조건** — 설치 없는 웹. 학교 폰 금지 때문에 접는 학교가 많다.
- **고1 리스크 정면 대응** — 숨기지 말고 FAQ에서 인정한 뒤 "계약서 없음·무료"로 해소한다.
- **근거의 한계 자진 고지** — 신뢰 섹션의 "모든 학생에게 맞지는 않았습니다" 한 줄과
  그 아래 "설문 전체 데이터 보기" 접기(부정 응답·"부풀리지 않겠습니다" 포함).
  숫자를 과장하지 않는다는 신호가 나머지 숫자를 믿게 만든다. 8번의 "숫자를 두는 층" 참고.
- **문의 후 무슨 일이 일어나는지** — 영업 전화 경계심을 먼저 풀어준다.

## 10. 연락처 / 브랜드 표기

- 이메일: ranha.projects@gmail.com
- Instagram: @r.xanha
- 푸터 표기: "운영: 타이거오키드" (주인공 브랜드는 WORD QUEST)
